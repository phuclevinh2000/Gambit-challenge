export interface modbusDataType {
  id: number;
  rawData: number;
}

export interface tableDataType {
  Note: string;
  format: string;
  id: number;
  name: string;
  number: number;
  rawData: number;
}
